name: Extract iFlow IDs from CPI Package

on:
  workflow_dispatch:
    inputs:
      package_id:
        description: 'Technical Package ID'
        required: true
        type: string

permissions:
  contents: write

jobs:
  extract-iflow-ids:
    runs-on: ubuntu-latest
    steps:
      # 1. OAuth2 Token holen
      - name: Get OAuth Token
        env:
          UAA_TOKEN_URL:    ${{ secrets.UAA_TOKEN_URL }}
          UAA_CLIENT_ID:    ${{ secrets.CPI_USER }}
          UAA_CLIENT_SECRET: ${{ secrets.CPI_PASSWORD }}
        run: |
          echo "Fetching OAuth token..."
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST "$UAA_TOKEN_URL" \
            -H "Content-Type: application/x-www-form-urlencoded" \
            -u "$UAA_CLIENT_ID:$UAA_CLIENT_SECRET" \
            -d "grant_type=client_credentials")
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | sed '$d')
          if [ "$HTTP_CODE" -ne 200 ]; then
            echo "Token-Request fehlgeschlagen (HTTP $HTTP_CODE)"
            echo "$BODY"
            exit 1
          fi
          echo "$BODY" | jq -r '.access_token' > oauth_token.txt
          echo "Token erhalten (Länge: $(wc -c < oauth_token.txt))"

      # 2. GET Designtime Artifacts
      - name: Get Designtime Artifacts
        env:
          CPI_BASE_URL: ${{ secrets.CPI_BASE_URL }}
          PACKAGE_ID:   ${{ github.event.inputs.package_id }}
        run: |
          set -e
          echo "Package ID        : $PACKAGE_ID"
          echo "Base URL          : $CPI_BASE_URL"
          echo "Target URL        : $CPI_BASE_URL/api/v1/IntegrationPackages('$PACKAGE_ID')/IntegrationDesigntimeArtifacts"
          TOKEN=$(cat oauth_token.txt)
          echo "Token length      : ${#TOKEN} Zeichen"

          curl --fail-with-body \
            -w "\n---HTTP_STATUS:%{http_code}\n---EFFECTIVE_URL:%{url_effective}\n---SIZE:%{size_download}\n" \
            -H "Authorization: Bearer $TOKEN" \
            -H "Accept: application/xml" \
            "$CPI_BASE_URL/api/v1/IntegrationPackages('$PACKAGE_ID')/IntegrationDesigntimeArtifacts" \
            -o response_body.xml \
            --dump-header response_headers.txt \
            --trace-ascii curl_trace.log

          CURL_EXIT=$?
          echo "curl exit code    : $CURL_EXIT"

          HTTP_STATUS=$(grep "^---HTTP_STATUS:" curl_trace.log | cut -d: -f2 | tr -d '[:space:]' || echo "000")
          echo "HTTP Status       : $HTTP_STATUS"

          echo ""
          echo "=== Response Headers (Auszug) ==="
          head -n 20 response_headers.txt || echo "(keine Headers)"
          echo ""
          echo "=== Letzte 400 Bytes Body ==="
          tail -c 400 response_body.xml 2>/dev/null || echo "(Datei leer oder nicht existent)"

          if [ "$CURL_EXIT" -ne 0 ] || [ "$HTTP_STATUS" -lt 200 ] || [ "$HTTP_STATUS" -gt 299 ]; then
            echo "Request FEHLGESCHLAGEN – HTTP $HTTP_STATUS"
            cat response_body.xml || echo "(leer)"
            tail -n 60 curl_trace.log || cat curl_trace.log
            exit 1
          fi

          mv response_body.xml response.xml
          echo "Erfolgreich gespeichert → response.xml ($(wc -c < response.xml) Bytes)"
          head -n 12 response.xml || echo "(konnte XML-Head nicht anzeigen)"

      # 3. Python-Extraktion
      - name: Extract iFlow IDs and Versions (Python)
        run: |
          python3 - <<'PY'
          # ... dein Python-Code unverändert ...
          PY
          echo ""
          echo "======================================"
          echo "Inhalt von artifacts.txt:"
          cat artifacts.txt || echo "(Datei leer oder nicht vorhanden)"
          echo "======================================"

      # 4. Matrix generieren
      - name: Generate matrix from artifacts.txt
        id: generate-matrix
        run: |
          if [ ! -s artifacts.txt ]; then
            echo "artifacts.txt ist leer → nichts zu transportieren"
            echo "matrix={\"include\":[]}" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          jq -R -s -c '
            split("\n")
            | map(select(length > 0 and (startswith(" ") | not)))
            | map(split(":") | select(length == 2))
            | map({
                iFlowId:       .[0] | gsub("\\s+$"; ""),
                artifactVersion: .[1] | gsub("^\\s+"; ""),
                packageID:     "${{ inputs.package_id }}"
              })
            | {include: .}
          ' artifacts.txt > matrix.json

          MATRIX_CONTENT=$(cat matrix.json)
          echo "Generated matrix:"
          echo "$MATRIX_CONTENT" | jq .
          echo "matrix=$MATRIX_CONTENT" >> "$GITHUB_OUTPUT"

      # 5. Matrix-Job für Transport (separater Job!)
      transport-all:
        name: Transport iFlows to QAS     
        needs: [extract-iflow-ids, generate-matrix]
        if: ${{ steps.generate-matrix.outputs.matrix != '{"include":[]}' }}
        strategy:
          fail-fast: false
          max-parallel: 3
          matrix: ${{ fromJson(needs.generate-matrix.outputs.matrix) }}
        uses: ./.github/workflows/transportScenarioArtifacts.yml
        with:
          iFlowId:         ${{ matrix.iFlowId }}
          artifactVersion: ${{ matrix.artifactVersion }}
          packageID:       ${{ matrix.packageID }}
        secrets: inherit
